// Slideshow timer management — currently handled inline in app.rs via WM_TIMER.
// This module will hold more complex slideshow logic (pause state persistence,
// reload timers, etc.) as they are implemented in later phases.
